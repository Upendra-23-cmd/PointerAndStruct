* Question : Write a program to Create a file metadata struct (size, type, permissions).

==> In this question we struct to store similar data ,becuase file have relateable metadata we group them together as if handle seperately we would have to handle 3 seperate variable with is not good practise

What we take in file metadata, 
                                 So, the field are Size, file type, permission

To extract this information we use "OS package"